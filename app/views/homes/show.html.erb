<!DOCTYPE html>
<html>
<head>
  <title>SelectedHouse</title>
 
 </head>
 <%= link_to(@home.title, target: "_blank") %><br/>
<small>Submitted <%= time_ago_in_words(@home.created_at) %> ago</small>

<h3>Comments</h3>

<%= form_for [@home, Comment.new] do |f| %>
<%= f.text_area :body, placeholder: "Add a comment" %><br/>
<%= f.submit "Add Comment" %>
<% end %>

<ul>
  <%= render(partial: 'comments/comment', collection: @home.comments) %>
</ul>
 
 
 
 </html>
 